== README

The app is build using Ruby on Rails and Angularjs


* Ruby version: Ruby 2.1

* Initialization/Dependencies: Run 
```
bundle install
```

* Database creation
```
rake db:migrate
```
* How to run the test suite (Only test the model)
```
bundle exec rspec
```

* To run de app just use the rails development server
```
rails server
```
